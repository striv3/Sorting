#ifndef RADIX_S
#define RADIX_S
#include<vector>
using std::vector;
vector<vector<unsigned> > CONVERT(const vector<unsigned>&A)
vector<unsigned> RADIX_SORT(const vector<unsigned>&);
void COUNTING_S(vector<unsigned>&,vector<unsigned>&,unsigned);
#endif // RADIX_s
